#include "Graph.h"
#include "Node.h"
#include "Edge.h"
#include <stdio.h>

int main(int argc, char* argv[]){
    
    // Zeiger auf Datei
    FILE *f;
    
    // Datei oeffnen
    f = fopen("graph.dot", "rwb");
    if (f)
    {
        // TODO Grah ausgeben
        fclose(f);
    }
    
    return 0;
}



